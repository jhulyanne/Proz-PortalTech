function operacoes(a, b) {
    console.log(`${a} mais" ${b} é igual à ${a + b}`);
    console.log(`${a} menos ${b} é igual à ${a - b}`);
    console.log(`${a} vezes ${b} é igual à ${a * b}`);
    console.log(`${a} dividido por ${b} é igual à ${a / b}`);
}

    operacoes(10, 20);
